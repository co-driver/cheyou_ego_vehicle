<!-- -->
<launch>
  <arg name='host' default='localhost'/>
  <arg name='port' default='2000'/>
  <arg name="vehicle_filter" default="co-driver*" />
  <arg name="sensor_definition_file" default="$(find cheyou_ego_vehicle)/config/sensors.json"/>
  <!-- use comma separated format "x,y,z,roll,pitch,yaw" -->
  <arg name="spawn_point" value ="45.44,60.92,0.5,0.0,0.0,180.0"/>

  <param name="/carla/host" value="$(arg host)" />
  <param name="/carla/port" value="$(arg port)" />

  <node pkg="cheyou_ego_vehicle" type="cheyou_ego_vehicle.py" name="cheyou_ego_vehicle" output="screen">
    <param name="sensor_definition_file" value="$(arg sensor_definition_file)" />
    <param name="vehicle_filter" value="$(arg vehicle_filter)" />
    <param name="spawn_point" value="$(arg spawn_point)" />
  </node>
</launch>

